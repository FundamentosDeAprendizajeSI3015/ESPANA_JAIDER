# ─────────────────────────────────────────────────────────────────
# Requirements – Pipeline ML: Clustering + Supervisado
# 10,000 Empresas más Grandes del País
# ─────────────────────────────────────────────────────────────────
# Instalación recomendada (entorno virtual):
#   python -m venv .venv
#   source .venv/bin/activate        # Linux/Mac
#   .venv\Scripts\activate           # Windows
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Core científico
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11

# Visualización
matplotlib>=3.7
seaborn>=0.13

# Machine Learning
scikit-learn>=1.4

# Fuzzy C-Means (opcional – si no está disponible se omite automáticamente)
scikit-fuzzy>=0.4.2

# Utilidades opcionales (mejoran el rendimiento pero no son obligatorias)
joblib>=1.3          # paralelismo (ya incluido en scikit-learn)
tqdm>=4.66           # barras de progreso en loops largos